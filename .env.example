# Copy to .env.local for development, and set these in Vercel/Netlify for production.

# Canonical site URL — used for canonical tags, sitemap, robots and OG images.
# No trailing slash. Must be the real production domain before launch.
NEXT_PUBLIC_SITE_URL=https://reputera.in

# ---------------------------------------------------------------------------
# Lead delivery — set AT LEAST ONE of the options below, or form submissions
# are validated and logged but never delivered anywhere.
# ---------------------------------------------------------------------------

# Option A: POST each submission as JSON to a webhook (Zapier, Make, n8n,
# Slack incoming webhook, or your own CRM endpoint).
# LEAD_WEBHOOK_URL=https://hooks.zapier.com/hooks/catch/000000/abcdef

# Option B: email each submission via Resend (https://resend.com).
# Both are required together.
# RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx
# LEAD_EMAIL_FROM=website@reputera.in
# Optional — forms already deliver to info@reputera.in without this.
# LEAD_EMAIL_TO=info@reputera.in

# ---------------------------------------------------------------------------
# WhatsApp bot (Meta WhatsApp Cloud API), on the company number
# +91 90822 17597. Without these the site's click-to-chat links still work —
# only the automated replies need configuring.
#
# Webhook callback URL: https://<your-domain>/api/whatsapp
# Subscribe the app to the `messages` field.
# ---------------------------------------------------------------------------

# Any string you choose; paste the same value into Meta's webhook setup.
# WHATSAPP_VERIFY_TOKEN=pick-a-long-random-string

# Permanent access token for the Meta app.
# WHATSAPP_TOKEN=EAAG...

# The registered number's ID (not the phone number itself).
# WHATSAPP_PHONE_NUMBER_ID=123456789012345

# App secret — used to verify each webhook really came from Meta.
# Deliveries are rejected without it.
# WHATSAPP_APP_SECRET=abc123...
